name: Flutter Cloud Injection Build
on:
  workflow_dispatch:

env:
  # 必须和 MainActivity.kt 里的 package 保持一致
  ORG_NAME: "com.example"
  PROJECT_NAME: "lofter_fixer"
  APP_CHINESE_NAME: "LOFTER去水印"
  FILE_NAME: "LofterFixer_Release"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.0'
          channel: 'stable'

      # ----------------------------------------------------------------
      # 🏗️ 核心步骤 1：云端造壳
      # ----------------------------------------------------------------
      - name: Generate Scaffolding
        run: |
          # 1. 如果本地存在旧的 android 文件夹，先删掉，防止干扰
          rm -rf android
          
          # 2. 让 Flutter 官方命令生成最标准的安卓工程
          # 这会自动生成正确的 Gradle Wrapper (gradlew) 和目录结构
          flutter create . --org ${{ env.ORG_NAME }} --project-name ${{ env.PROJECT_NAME }} --platforms android -a kotlin
          
          echo "✅ 标准骨架已生成"

      # ----------------------------------------------------------------
      # 💉 核心步骤 2：注入灵魂 (关键！)
      # ----------------------------------------------------------------
      - name: Inject Custom Code
        run: |
          echo "🚚 正在将 android_overlay 的内容注入到 android 目录..."
          
          # 强制覆盖 (-f), 递归 (-r)
          # 把我们准备好的核心代码，覆盖到刚刚生成的骨架里
          cp -rf android_overlay/* android/
          
          echo "✅ 代码注入完毕！"

      # ----------------------------------------------------------------
      # 🔧 后续常规步骤
      # ----------------------------------------------------------------
      - name: Generate Icon
        run: |
          flutter pub get
          dart run flutter_launcher_icons

      - name: Set App Name
        run: |
          MANIFEST="android/app/src/main/AndroidManifest.xml"
          if [ -f "$MANIFEST" ]; then
            sed -i 's/android:label=".*"/android:label="${{ env.APP_CHINESE_NAME }}"/g' $MANIFEST
          fi

      - name: Generate Keystore
        run: |
          keytool -genkey -v -keystore upload-keystore.jks \
            -keyalg RSA -keysize 2048 -validity 10000 \
            -alias upload -dname "CN=Android, OU=Android, O=Android, L=Android, S=Android, C=US" \
            -storepass android -keypass android
          
          echo "storePassword=android" > android/key.properties
          echo "keyPassword=android" >> android/key.properties
          echo "keyAlias=upload" >> android/key.properties
          echo "storeFile=../../upload-keystore.jks" >> android/key.properties

      - name: Build APK
        run: flutter build apk --release --target-platform android-arm64

      - name: Rename and Upload
        run: mv build/app/outputs/flutter-apk/app-release.apk ${{ env.FILE_NAME }}.apk

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.FILE_NAME }}
          path: ${{ env.FILE_NAME }}.apk